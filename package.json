{
  "scripts": {
    "benchmark": "bun scripts/benchmark.mjs",
    "build": "bun run crepus -- webext build --app .",
    "crepus": "cargo run --manifest-path ../crepuscularity/Cargo.toml -p crepuscularity-cli --",
    "format": "cargo fmt --check",
    "lint": "cargo clippy --all-targets --all-features -- -D warnings",
    "test": "cargo test"
  }
}
