--- Popup
div vc-popup
  div vc-header
    h1 vc-title
      "{title}"
    div vc-status
      "{status}"
  div vc-grid
    for group in {groups}
      div vc-group
        div vc-group-title
          "{name}"
        for item in {items}
          div vc-row
            span vc-keys
              "{keys}"
            span vc-label
              "{label}"
  div vc-footer
    "Press ? on a page for the in-page help overlay."
